# AccidentGuard Frontend — React + Vite

## Setup
1. Install Node.js 18+.
2. Copy `.env.example` to `.env` and set `VITE_API_URL` to the FastAPI server URL.
3. Run `npm install`.
4. Run `npm run dev`.

Production build: `npm run build`.
